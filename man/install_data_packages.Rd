% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_data_packages.R
\name{install_data_packages}
\alias{install_data_packages}
\title{Install the cori.data.* companion packages}
\usage{
install_data_packages(
  packages = c("cori.data.bds", "cori.data.bfs", "cori.data.bps", "cori.data.fcc",
    "cori.data.hu", "cori.data.pep", "cori.data.qcew"),
  ...
)
}
\arguments{
\item{packages}{Character vector of package names to install. Default:
all seven \verb{cori.data.*} companion packages.}

\item{...}{Additional arguments passed to \code{remotes::install_github()}.}
}
\value{
Invisibly, the character vector of packages installed.
}
\description{
Installs the \verb{cori.data.*} packages that provide read/write access to
specific public data sources on top of \code{cori.data}: Business Dynamics
Statistics, Business Formation Statistics, Building Permits Survey, FCC
broadband deployment data, Housing Unit estimates, Population Estimates,
and Quarterly Census of Employment and Wages.
}
\details{
These are declared in \code{Suggests}, not \code{Imports}/\code{Depends}, because most of
them already depend on \code{cori.data} (for \code{\link[cori.data.s3:connect_to_s3]{cori.data.s3::connect_to_s3()}}) -- listing them
as hard dependencies of \code{cori.data} itself would create a circular
dependency that R's namespace loader cannot resolve. This function is the
explicit, one-line alternative: run it right after installing \code{cori.data}
to pull in the rest of the suite.
}
\examples{
\dontrun{
cori.data::install_data_packages()

# Install just one or two
cori.data::install_data_packages(c("cori.data.bds", "cori.data.qcew"))
}

}
